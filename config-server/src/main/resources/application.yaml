server:
  port: 9296

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/venkatdhruv/app-config
          clone-on-start: true
          default-label: main
          username: venkatdhruv
          password: ${GIT_TOKEN}